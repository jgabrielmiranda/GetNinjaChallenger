{
  "name": "server",
  "version": "1.0.0",
  "description": "Servidor para consultar os dados de fields.json",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gabriel Miranda",
  "license": "ISC",
  "dependencies": {
    "mocha": "^5.2.0"
  }
}